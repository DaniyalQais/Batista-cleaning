# Batista-cleaning

Premium cleaning service demo with live scope builder, real-time estimate dashboard, and quote request flow.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   ```bash
   npm install
   ```
2. Run the app:
   ```bash
   npm run dev
   ```

The app runs at `http://localhost:3000`.
